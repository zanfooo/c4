/*Dato N un numero intero positivo maggiore di 1, generare e visualizzare
il numero precedente. */
#include <stdio.h>

int main(int argc, char* argv[])
{
    int n;
    int nMod;
    do{
    printf("Inserisci il numero N interno positivo: ");
    scanf("%d",&n);
    }while(n<0);
    
    nMod = n - 1;
    
    printf("Il numero precedente a %d è: %d", n, nMod);

    return 0;
}
