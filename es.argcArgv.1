/*Scrivi un programma che legge da argv[1] un numero intero positivo (N) e poi
disegna a terminale un quadrato vuoto composto di asterischi (‘*’)
con il lato lungo N:

Per N pari a 3 il programma stampa:

***
* *
***
Per N pari a 5 il programma stampa:

*****
*   *
*   *
*   *
******/
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    if (argc != 2) {
        printf("Numero di argomenti errato: %d\n", argc);
        exit(1);
    }

    int n = atoi(argv[1]);

    // Controllo se N è un numero intero positivo
    if (n <= 0) {
        printf("Errore: il numero deve essere un intero positivo.\n");
        exit(1);
    }

    // Disegna il quadrato vuoto
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            if (i == 1 || i == n || j == 1 || j == n) {
                printf("*");
            } else {
                printf(" ");
            }
        }
        printf("\n");
    }

    return 0;
}
